import { MailBckfilePipe } from './mail-bckfile.pipe';

describe('MailBckfilePipe', () => {
  it('create an instance', () => {
    const pipe = new MailBckfilePipe();
    expect(pipe).toBeTruthy();
  });
});
