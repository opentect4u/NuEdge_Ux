import { StickyHeaderDirectiveDirective } from './sticky-header-directive.directive';

describe('StickyHeaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyHeaderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
